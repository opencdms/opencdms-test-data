-- DROP TYPE grid_ref_type_enum;

CREATE TYPE grid_ref_type_enum AS ENUM (
	'CI',
	'IRL',
	'OS',
	'XX',
	'ROI'
);

-- DROP TYPE prime_capability_flag;

CREATE TYPE prime_capability_flag AS ENUM (
	'T',
	'F'
);

-- DROP TYPE prime_capability_flag_enum;

CREATE TYPE prime_capability_flag_enum AS ENUM (
	'T',
	'F'
);
